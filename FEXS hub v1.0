-- FEXS GUI Hub - Draggable Speed Toggle GUI

local player = game.Players.LocalPlayer
local screenGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
screenGui.Name = "FEXSGUI"
screenGui.ResetOnSpawn = false

-- Create Frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 220, 0, 160)
frame.Position = UDim2.new(0.05, 0, 0.1, 0)
frame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
frame.BorderSizePixel = 2
frame.Draggable = true
frame.Active = true
frame.Selectable = true
frame.Parent = screenGui

-- Title Label
local title = Instance.new("TextLabel")
title.Text = "FEXS GUI Hub"
title.Font = Enum.Font.SourceSansBold
title.TextSize = 22
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.BackgroundTransparency = 1
title.Size = UDim2.new(1, 0, 0, 40)
title.Parent = frame

-- Speed Toggle Button
local speedButton = Instance.new("TextButton")
speedButton.Text = "Toggle Speed"
speedButton.Font = Enum.Font.SourceSans
speedButton.TextSize = 20
speedButton.Size = UDim2.new(0.8, 0, 0, 40)
speedButton.Position = UDim2.new(0.1, 0, 0.5, 0)
speedButton.BackgroundColor3 = Color3.fromRGB(70, 150, 255)
speedButton.TextColor3 = Color3.fromRGB(255, 255, 255)
speedButton.Parent = frame

-- Speed Toggle Logic
local fastSpeed = 50
local normalSpeed = 16
local isFast = false

speedButton.MouseButton1Click:Connect(function()
	local character = player.Character or player.CharacterAdded:Wait()
	local humanoid = character:FindFirstChildOfClass("Humanoid")

	if humanoid then
		if isFast then
			humanoid.WalkSpeed = normalSpeed
			speedButton.Text = "Toggle Speed (Normal)"
		else
			humanoid.WalkSpeed = fastSpeed
			speedButton.Text = "Toggle Speed (Fast)"
		end
		isFast = not isFast
	end
end)
